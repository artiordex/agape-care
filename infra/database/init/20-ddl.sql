-- Description : 20-ddl.sql - ?? ?????? DDL ? ?? ????
-- Author : Shiwoo Min
-- Date : 2025-02-09
-- Schema : Web 알림마당 (공지사항, 게시판, 갤러리, 식단표, 프로그램)
-- Note : Extensions는 00-extensions.sql에서 이미 생성됨

-- ============================================
-- Utility Functions
-- ============================================

CREATE OR REPLACE FUNCTION set_updated_at()
RETURNS TRIGGER
LANGUAGE plpgsql
AS $$
BEGIN
  NEW.updated_at := now();
  RETURN NEW;
END;
$$;

COMMENT ON FUNCTION set_updated_at() IS 'UPDATE 시 updated_at 컬럼을 자동으로 현재 시각으로 갱신하는 트리거 함수';

-- ============================================
-- 1. 기본 테이블 (의존성)
-- ============================================

-- FACILITIES (시설 정보)
CREATE TABLE facilities (
  id                  BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  org_code            TEXT NOT NULL UNIQUE,
  facility_name       TEXT NOT NULL,
  facility_desc       TEXT,
  facility_type       TEXT NOT NULL,
  designated_date     DATE,
  director            TEXT NOT NULL,
  director_phone      TEXT,
  ceo_name            TEXT NOT NULL,
  business_no         TEXT NOT NULL,
  biz_type            TEXT,
  staff_count         INT,
  phone               TEXT NOT NULL,
  fax                 TEXT,
  email               TEXT,
  homepage            TEXT,
  zip                 TEXT,
  address1            TEXT NOT NULL,
  address2            TEXT,
  total_capacity      INT,
  short_stay_capacity INT,
  day_care_capacity   INT,
  stamp_image         TEXT,
  created_at          TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at          TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE facilities IS '시설 정보 - 요양원 기본 정보 및 설정';
COMMENT ON COLUMN facilities.id IS '시설 ID (Primary Key)';
COMMENT ON COLUMN facilities.org_code IS '기관기호 (고유값)';
COMMENT ON COLUMN facilities.facility_name IS '시설명';
COMMENT ON COLUMN facilities.facility_desc IS '시설 설명';
COMMENT ON COLUMN facilities.facility_type IS '시설 유형';
COMMENT ON COLUMN facilities.designated_date IS '지정일자';
COMMENT ON COLUMN facilities.director IS '시설장명';
COMMENT ON COLUMN facilities.director_phone IS '시설장 연락처';
COMMENT ON COLUMN facilities.ceo_name IS '대표자명';
COMMENT ON COLUMN facilities.business_no IS '사업자등록번호';
COMMENT ON COLUMN facilities.biz_type IS '업태/종목';
COMMENT ON COLUMN facilities.staff_count IS '종사자 수';
COMMENT ON COLUMN facilities.phone IS '대표 전화번호';
COMMENT ON COLUMN facilities.fax IS '팩스 번호';
COMMENT ON COLUMN facilities.email IS '이메일';
COMMENT ON COLUMN facilities.homepage IS '홈페이지';
COMMENT ON COLUMN facilities.zip IS '우편번호';
COMMENT ON COLUMN facilities.address1 IS '주소';
COMMENT ON COLUMN facilities.address2 IS '상세주소';
COMMENT ON COLUMN facilities.total_capacity IS '정원 (전체)';
COMMENT ON COLUMN facilities.short_stay_capacity IS '단기보호 정원';
COMMENT ON COLUMN facilities.day_care_capacity IS '주야간보호 정원';
COMMENT ON COLUMN facilities.stamp_image IS '직인 이미지 URL';
COMMENT ON COLUMN facilities.created_at IS '생성일시';
COMMENT ON COLUMN facilities.updated_at IS '수정일시';

CREATE TRIGGER trg_facilities_u
BEFORE UPDATE ON facilities
FOR EACH ROW EXECUTE FUNCTION set_updated_at();



-- DEPARTMENTS (부서)
CREATE TABLE departments (
  id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  code        TEXT NOT NULL UNIQUE,
  name        TEXT NOT NULL,
  description TEXT,
  is_active   BOOLEAN NOT NULL DEFAULT TRUE,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE departments IS '부서 정보 - 조직 내 부서 구조를 정의하는 마스터 테이블';
COMMENT ON COLUMN departments.id IS '부서 ID (Primary Key)';
COMMENT ON COLUMN departments.code IS '부서 코드 (고유값)';
COMMENT ON COLUMN departments.name IS '부서명';
COMMENT ON COLUMN departments.description IS '부서 설명';
COMMENT ON COLUMN departments.is_active IS '활성화 여부';
COMMENT ON COLUMN departments.created_at IS '생성일시';
COMMENT ON COLUMN departments.updated_at IS '수정일시';

CREATE TRIGGER trg_departments_u
BEFORE UPDATE ON departments
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- EMPLOYEE_ROLES (직원 역할)
CREATE TABLE employee_roles (
  id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  code        TEXT NOT NULL UNIQUE,
  name        TEXT NOT NULL,
  description TEXT,
  permissions JSONB NOT NULL DEFAULT '{}'::jsonb,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE employee_roles IS '직원 역할/권한 정보 - 직원의 역할과 권한을 정의하는 마스터 테이블';
COMMENT ON COLUMN employee_roles.id IS '역할 ID (Primary Key)';
COMMENT ON COLUMN employee_roles.code IS '역할 코드 (고유값)';
COMMENT ON COLUMN employee_roles.name IS '역할명';
COMMENT ON COLUMN employee_roles.description IS '역할 설명';
COMMENT ON COLUMN employee_roles.permissions IS '권한 설정 (JSON)';
COMMENT ON COLUMN employee_roles.created_at IS '생성일시';
COMMENT ON COLUMN employee_roles.updated_at IS '수정일시';

CREATE TRIGGER trg_employee_roles_u
BEFORE UPDATE ON employee_roles
FOR EACH ROW EXECUTE FUNCTION set_updated_at();



-- EMPLOYEES (직원)
CREATE TABLE employees (
  id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  department_id   BIGINT REFERENCES departments(id) ON DELETE SET NULL,
  role_id         BIGINT REFERENCES employee_roles(id) ON DELETE SET NULL,
  email           CITEXT UNIQUE,
  password_hash   TEXT,
  name            TEXT NOT NULL,
  phone_number    TEXT,
  hire_date       DATE,
  resign_date     DATE,
  status          TEXT NOT NULL DEFAULT 'ACTIVE',
  is_admin        BOOLEAN NOT NULL DEFAULT FALSE,
  meta            JSONB NOT NULL DEFAULT '{}'::jsonb,
  last_login_at   TIMESTAMPTZ,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT employees_status_ck
    CHECK (status IN ('ACTIVE','ON_LEAVE','INACTIVE'))
);

COMMENT ON TABLE employees IS '직원 정보 - 시스템 사용자 및 요양원 직원 정보를 관리하는 핵심 테이블';
COMMENT ON COLUMN employees.id IS '직원 ID (Primary Key)';
COMMENT ON COLUMN employees.department_id IS '부서 ID (외래키) - departments 테이블 참조';
COMMENT ON COLUMN employees.role_id IS '역할 ID (외래키) - employee_roles 테이블 참조';
COMMENT ON COLUMN employees.email IS '이메일 (로그인 ID, 고유값, 대소문자 구분 안함)';
COMMENT ON COLUMN employees.password_hash IS '비밀번호 해시 (민감정보)';
COMMENT ON COLUMN employees.name IS '직원명';
COMMENT ON COLUMN employees.phone_number IS '전화번호';
COMMENT ON COLUMN employees.hire_date IS '입사일';
COMMENT ON COLUMN employees.resign_date IS '퇴사일';
COMMENT ON COLUMN employees.status IS '재직 상태 - ACTIVE: 재직중, ON_LEAVE: 휴직중, INACTIVE: 퇴사';
COMMENT ON COLUMN employees.is_admin IS '관리자 여부';
COMMENT ON COLUMN employees.meta IS '추가 메타데이터 (JSON)';
COMMENT ON COLUMN employees.last_login_at IS '마지막 로그인 일시';
COMMENT ON COLUMN employees.created_at IS '생성일시';
COMMENT ON COLUMN employees.updated_at IS '수정일시';

CREATE TRIGGER trg_employees_u
BEFORE UPDATE ON employees
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- EMPLOYEE_PERMISSIONS (직원 개인 권한)
CREATE TABLE employee_permissions (
  id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  employee_id BIGINT NOT NULL REFERENCES employees(id) ON DELETE CASCADE,
  role_id     BIGINT REFERENCES employee_roles(id) ON DELETE SET NULL,
  permissions JSONB NOT NULL DEFAULT '{}'::jsonb,
  updated_by  BIGINT REFERENCES employees(id) ON DELETE SET NULL,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT employee_permissions_uniq UNIQUE (employee_id)
);

COMMENT ON TABLE employee_permissions IS '직원 개인 권한 - 역할 외의 추가/예외 권한 설정';
COMMENT ON COLUMN employee_permissions.id IS '권한 ID (Primary Key)';
COMMENT ON COLUMN employee_permissions.employee_id IS '직원 ID (외래키) - employees 테이블 참조';
COMMENT ON COLUMN employee_permissions.role_id IS '역할 ID (외래키, 선택) - 특정 역할 기준 오버라이드일 때';
COMMENT ON COLUMN employee_permissions.permissions IS '권한 설정 (JSON)';
COMMENT ON COLUMN employee_permissions.updated_by IS '수정자 ID (외래키) - employees 테이블 참조';
COMMENT ON COLUMN employee_permissions.created_at IS '생성일시';
COMMENT ON COLUMN employee_permissions.updated_at IS '수정일시';

CREATE TRIGGER trg_employee_permissions_u
BEFORE UPDATE ON employee_permissions
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- FILE_STORAGE (파일 저장소)
CREATE TABLE file_storage (
  id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  bucket          TEXT NOT NULL DEFAULT 'default',
  path            TEXT NOT NULL,
  original_name   TEXT,
  mime_type       TEXT,
  size_bytes      BIGINT,
  checksum        TEXT,
  created_by      BIGINT REFERENCES employees(id) ON DELETE SET NULL,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE file_storage IS '파일 저장소 - S3, 로컬 등에 저장된 파일의 메타데이터 관리';
COMMENT ON COLUMN file_storage.id IS '파일 ID (Primary Key)';
COMMENT ON COLUMN file_storage.bucket IS '저장소 버킷';
COMMENT ON COLUMN file_storage.path IS '파일 경로 - 버킷 내 상대 경로';
COMMENT ON COLUMN file_storage.original_name IS '원본 파일명';
COMMENT ON COLUMN file_storage.mime_type IS 'MIME 타입';
COMMENT ON COLUMN file_storage.size_bytes IS '파일 크기 (바이트)';
COMMENT ON COLUMN file_storage.checksum IS '체크섬 (MD5, SHA256 등)';
COMMENT ON COLUMN file_storage.created_by IS '업로드자 ID (외래키) - employees 테이블 참조';
COMMENT ON COLUMN file_storage.created_at IS '생성일시';

CREATE UNIQUE INDEX uq_file_storage_bucket_path
  ON file_storage(bucket, path);

-- SITE_INFOS (사이트 정보)
CREATE TABLE site_infos (
  id               BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  service_name     TEXT NOT NULL,
  service_desc     TEXT,
  contact_phone    TEXT,
  contact_email    TEXT,
  customer_hours   TEXT,
  meta_title       TEXT,
  meta_description TEXT,
  meta_keywords    TEXT,
  footer_text      TEXT,
  legal_notice     TEXT,
  updated_by       BIGINT REFERENCES employees(id) ON DELETE SET NULL,
  created_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at       TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE site_infos IS '사이트 정보 - 서비스 기본 설정 및 SEO 정보';
COMMENT ON COLUMN site_infos.id IS '사이트 정보 ID (Primary Key)';
COMMENT ON COLUMN site_infos.service_name IS '서비스명 (사이트 제목)';
COMMENT ON COLUMN site_infos.service_desc IS '서비스 설명';
COMMENT ON COLUMN site_infos.contact_phone IS '대표 연락처';
COMMENT ON COLUMN site_infos.contact_email IS '대표 이메일';
COMMENT ON COLUMN site_infos.customer_hours IS '고객센터 운영시간';
COMMENT ON COLUMN site_infos.meta_title IS '메타 제목 (SEO)';
COMMENT ON COLUMN site_infos.meta_description IS '메타 설명 (SEO)';
COMMENT ON COLUMN site_infos.meta_keywords IS '메타 키워드 (SEO)';
COMMENT ON COLUMN site_infos.footer_text IS '푸터 텍스트 (Copyright)';
COMMENT ON COLUMN site_infos.legal_notice IS '법적 고지';
COMMENT ON COLUMN site_infos.updated_by IS '수정자 ID (외래키) - employees 테이블 참조';
COMMENT ON COLUMN site_infos.created_at IS '생성일시';
COMMENT ON COLUMN site_infos.updated_at IS '수정일시';

CREATE TRIGGER trg_site_infos_u
BEFORE UPDATE ON site_infos
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- RESIDENTS (입소자 정보)
CREATE TABLE residents (
  id                BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  code              TEXT NOT NULL UNIQUE,
  name              TEXT NOT NULL,
  birthday          DATE,
  gender            TEXT,
  national_id_hash  TEXT,
  phone_number      TEXT,
  address_zipcode   TEXT,
  address_road      TEXT,
  address_detail    TEXT,
  admission_date    DATE,
  discharge_date    DATE,
  discharge_reason  TEXT,
  status            TEXT NOT NULL DEFAULT 'ADMITTED',
  guardian_name     TEXT,
  guardian_phone    TEXT,
  profile_file_id   BIGINT REFERENCES file_storage(id) ON DELETE SET NULL,
  memo              TEXT,
  meta              JSONB NOT NULL DEFAULT '{}'::jsonb,
  deleted_at        TIMESTAMPTZ,
  created_at        TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at        TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT residents_gender_ck
    CHECK (gender IS NULL OR gender IN ('M','F','OTHER')),
  CONSTRAINT residents_status_ck
    CHECK (status IN ('ADMITTED','ON_LEAVE','DISCHARGED','PENDING'))
);

COMMENT ON TABLE residents IS '입소자 정보 - 요양원 입소자의 기본 정보를 관리하는 핵심 테이블';
COMMENT ON COLUMN residents.id IS '입소자 ID (Primary Key)';
COMMENT ON COLUMN residents.code IS '입소자 코드 (고유값) - 내부 관리번호';
COMMENT ON COLUMN residents.name IS '입소자명';
COMMENT ON COLUMN residents.birthday IS '생년월일';
COMMENT ON COLUMN residents.gender IS '성별 - M: 남성, F: 여성, OTHER: 기타';
COMMENT ON COLUMN residents.national_id_hash IS '주민등록번호 해시 (개인정보 보호)';
COMMENT ON COLUMN residents.admission_date IS '입소일';
COMMENT ON COLUMN residents.discharge_date IS '퇴소일';
COMMENT ON COLUMN residents.status IS '입소 상태 - ADMITTED: 입소중, ON_LEAVE: 외박중, DISCHARGED: 퇴소, PENDING: 입소대기';
COMMENT ON COLUMN residents.guardian_name IS '보호자명';
COMMENT ON COLUMN residents.guardian_phone IS '보호자 연락처';
COMMENT ON COLUMN residents.memo IS '메모';
COMMENT ON COLUMN residents.meta IS '추가 메타데이터 (JSON)';
COMMENT ON COLUMN residents.created_at IS '생성일시';
COMMENT ON COLUMN residents.updated_at IS '수정일시';

COMMENT ON COLUMN residents.phone_number IS '입소자 연락처';
COMMENT ON COLUMN residents.address_zipcode IS '주소 우편번호';
COMMENT ON COLUMN residents.address_road IS '주소(도로명)';
COMMENT ON COLUMN residents.address_detail IS '주소 상세';
COMMENT ON COLUMN residents.discharge_reason IS '퇴소 사유';
COMMENT ON COLUMN residents.profile_file_id IS '프로필 파일 ID (FK - file_storage.id)';
COMMENT ON COLUMN residents.deleted_at IS '삭제일시 (soft delete)';

CREATE INDEX idx_residents_status
  ON residents(status, admission_date DESC)
  WHERE deleted_at IS NULL;

CREATE INDEX idx_residents_name
  ON residents(name)
  WHERE deleted_at IS NULL;

CREATE TRIGGER trg_residents_u
BEFORE UPDATE ON residents
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- ============================================
-- 2. 공지사항 (2 Tables)
-- ============================================

-- NOTICES (공지사항)
CREATE TABLE notices (
  id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  title         TEXT NOT NULL,
  content       TEXT NOT NULL,
  category      TEXT DEFAULT 'GENERAL',
  is_pinned     BOOLEAN NOT NULL DEFAULT FALSE,
  is_active     BOOLEAN NOT NULL DEFAULT TRUE,
  view_count    INT NOT NULL DEFAULT 0,
  published_at  TIMESTAMPTZ,
  created_by    BIGINT REFERENCES employees(id) ON DELETE SET NULL,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE notices IS '공지사항 - 시설 내부 공지 및 알림';
COMMENT ON COLUMN notices.id IS '공지 ID (Primary Key)';
COMMENT ON COLUMN notices.title IS '공지 제목';
COMMENT ON COLUMN notices.content IS '공지 내용 (HTML)';
COMMENT ON COLUMN notices.category IS '카테고리 - GENERAL: 일반, URGENT: 긴급, EVENT: 행사, EDUCATION: 교육, MAINTENANCE: 유지보수';
COMMENT ON COLUMN notices.is_pinned IS '상단 고정 여부';
COMMENT ON COLUMN notices.is_active IS '활성화 여부';
COMMENT ON COLUMN notices.view_count IS '조회수';
COMMENT ON COLUMN notices.published_at IS '게시일시';
COMMENT ON COLUMN notices.created_by IS '작성자 ID (외래키) - employees 테이블 참조';
COMMENT ON COLUMN notices.created_at IS '생성일시';
COMMENT ON COLUMN notices.updated_at IS '수정일시';

CREATE INDEX idx_notices_active_published
  ON notices(is_active, published_at DESC)
  WHERE is_active = true;

CREATE INDEX idx_notices_pinned_created
  ON notices(is_pinned DESC, created_at DESC)
  WHERE is_active = true;

CREATE TRIGGER trg_notices_u
BEFORE UPDATE ON notices
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- NOTICE_FILES (공지사항 첨부파일)
CREATE TABLE notice_files (
  id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  notice_id   BIGINT NOT NULL REFERENCES notices(id) ON DELETE CASCADE,
  file_id     BIGINT NOT NULL REFERENCES file_storage(id) ON DELETE CASCADE,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE notice_files IS '공지사항 첨부 파일 - 공지사항 관련 첨부 파일';
COMMENT ON COLUMN notice_files.id IS '연결 ID (Primary Key)';
COMMENT ON COLUMN notice_files.notice_id IS '공지 ID (외래키) - notices 테이블 참조';
COMMENT ON COLUMN notice_files.file_id IS '파일 ID (외래키) - file_storage 테이블 참조';
COMMENT ON COLUMN notice_files.created_at IS '생성일시';

CREATE INDEX idx_notice_files_notice
  ON notice_files(notice_id, created_at);

-- ============================================
-- 3. 게시판 (3 Tables)
-- ============================================

-- BOARD_POSTS (게시글)
CREATE TABLE board_posts (
  id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  board_key     TEXT NOT NULL,
  title         TEXT NOT NULL,
  content       TEXT NOT NULL,
  author_id     BIGINT REFERENCES employees(id) ON DELETE SET NULL,
  view_count    INT NOT NULL DEFAULT 0,
  is_pinned     BOOLEAN NOT NULL DEFAULT FALSE,
  is_locked     BOOLEAN NOT NULL DEFAULT FALSE,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE board_posts IS '게시판 게시글 - 자유게시판, 질문게시판, 건의게시판';
COMMENT ON COLUMN board_posts.id IS '게시글 ID (Primary Key)';
COMMENT ON COLUMN board_posts.board_key IS '게시판 구분 키 - FREE: 자유게시판, QNA: 질문게시판, SUGGESTION: 건의게시판, NOTICE: 공지게시판';
COMMENT ON COLUMN board_posts.title IS '게시글 제목';
COMMENT ON COLUMN board_posts.content IS '게시글 내용 (HTML)';
COMMENT ON COLUMN board_posts.author_id IS '작성자 ID (외래키) - employees 테이블 참조';
COMMENT ON COLUMN board_posts.view_count IS '조회수';
COMMENT ON COLUMN board_posts.is_pinned IS '상단 고정 여부';
COMMENT ON COLUMN board_posts.is_locked IS '잠금 여부 (댓글 작성 불가)';
COMMENT ON COLUMN board_posts.created_at IS '생성일시';
COMMENT ON COLUMN board_posts.updated_at IS '수정일시';

CREATE INDEX idx_board_posts_board_created
  ON board_posts(board_key, created_at DESC);

CREATE INDEX idx_board_posts_pinned
  ON board_posts(is_pinned DESC, created_at DESC);

CREATE INDEX idx_board_posts_author
  ON board_posts(author_id);

CREATE TRIGGER trg_board_posts_u
BEFORE UPDATE ON board_posts
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- BOARD_FILES (게시판 첨부파일)
CREATE TABLE board_files (
  id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  post_id     BIGINT NOT NULL REFERENCES board_posts(id) ON DELETE CASCADE,
  file_id     BIGINT NOT NULL REFERENCES file_storage(id) ON DELETE CASCADE,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE board_files IS '게시판 첨부 파일 - 게시글 첨부 파일';
COMMENT ON COLUMN board_files.id IS '연결 ID (Primary Key)';
COMMENT ON COLUMN board_files.post_id IS '게시글 ID (외래키) - board_posts 테이블 참조';
COMMENT ON COLUMN board_files.file_id IS '파일 ID (외래키) - file_storage 테이블 참조';
COMMENT ON COLUMN board_files.created_at IS '생성일시';

CREATE INDEX idx_board_files_post
  ON board_files(post_id, created_at);

-- BOARD_COMMENTS (댓글)
CREATE TABLE board_comments (
  id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  post_id         BIGINT NOT NULL REFERENCES board_posts(id) ON DELETE CASCADE,
  parent_id       BIGINT REFERENCES board_comments(id) ON DELETE CASCADE,
  author_id       BIGINT REFERENCES employees(id) ON DELETE SET NULL,
  content         TEXT NOT NULL,
  is_deleted      BOOLEAN NOT NULL DEFAULT FALSE,
  guest_nickname  TEXT,
  guest_password  TEXT,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT now(),

  -- author_id 없으면 guest_nickname/guest_password 필수
  CONSTRAINT chk_comment_author
    CHECK (
      author_id IS NOT NULL
      OR (guest_nickname IS NOT NULL AND guest_password IS NOT NULL)
    )
);

COMMENT ON TABLE board_comments IS '게시판 댓글 - 게시글에 대한 댓글 및 대댓글';
COMMENT ON COLUMN board_comments.id IS '댓글 ID (Primary Key)';
COMMENT ON COLUMN board_comments.post_id IS '게시글 ID (외래키) - board_posts 테이블 참조';
COMMENT ON COLUMN board_comments.parent_id IS '부모 댓글 ID (외래키) - 대댓글인 경우';
COMMENT ON COLUMN board_comments.author_id IS '작성자 ID (외래키) - employees 테이블 참조, 비회원이면 NULL';
COMMENT ON COLUMN board_comments.content IS '댓글 내용';
COMMENT ON COLUMN board_comments.is_deleted IS '삭제 여부';
COMMENT ON COLUMN board_comments.guest_nickname IS '비회원 닉네임 (비회원 댓글인 경우)';
COMMENT ON COLUMN board_comments.guest_password IS '비회원 비밀번호 해시 (비회원 댓글인 경우)';
COMMENT ON COLUMN board_comments.created_at IS '생성일시';
COMMENT ON COLUMN board_comments.updated_at IS '수정일시';

CREATE INDEX idx_board_comments_post
  ON board_comments(post_id, created_at)
  WHERE is_deleted = false;

CREATE INDEX idx_board_comments_parent
  ON board_comments(parent_id, created_at)
  WHERE parent_id IS NOT NULL AND is_deleted = false;

CREATE TRIGGER trg_board_comments_u
BEFORE UPDATE ON board_comments
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- ============================================
-- 4. 갤러리 (2 Tables)
-- ============================================

-- GALLERY_ITEMS (갤러리)
CREATE TABLE gallery_items (
  id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  title         TEXT,
  description   TEXT,
  category      TEXT DEFAULT 'GENERAL',
  event_date    DATE,
  created_by    BIGINT REFERENCES employees(id) ON DELETE SET NULL,
  is_public     BOOLEAN NOT NULL DEFAULT TRUE,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE gallery_items IS '갤러리 항목 - 사진첩, 행사 사진 등';
COMMENT ON COLUMN gallery_items.id IS '갤러리 항목 ID (Primary Key)';
COMMENT ON COLUMN gallery_items.title IS '제목';
COMMENT ON COLUMN gallery_items.description IS '설명';
COMMENT ON COLUMN gallery_items.category IS '카테고리 - GENERAL: 일반, EVENT: 행사, DAILY: 일상';
COMMENT ON COLUMN gallery_items.event_date IS '행사 일자';
COMMENT ON COLUMN gallery_items.created_by IS '작성자 ID (외래키) - employees 테이블 참조';
COMMENT ON COLUMN gallery_items.is_public IS '공개 여부';
COMMENT ON COLUMN gallery_items.created_at IS '생성일시';
COMMENT ON COLUMN gallery_items.updated_at IS '수정일시';

CREATE INDEX idx_gallery_items_public_event
  ON gallery_items(is_public, event_date DESC)
  WHERE is_public = true;

CREATE INDEX idx_gallery_items_category
  ON gallery_items(category, is_public, event_date DESC)
  WHERE is_public = true;

CREATE TRIGGER trg_gallery_items_u
BEFORE UPDATE ON gallery_items
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- GALLERY_FILES (갤러리 파일)
CREATE TABLE gallery_files (
  id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  gallery_id    BIGINT NOT NULL REFERENCES gallery_items(id) ON DELETE CASCADE,
  file_id       BIGINT NOT NULL REFERENCES file_storage(id) ON DELETE CASCADE,
  sort_order    INT NOT NULL DEFAULT 0,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE gallery_files IS '갤러리 파일 - 갤러리 항목에 포함된 이미지/동영상';
COMMENT ON COLUMN gallery_files.id IS '연결 ID (Primary Key)';
COMMENT ON COLUMN gallery_files.gallery_id IS '갤러리 ID (외래키) - gallery_items 테이블 참조';
COMMENT ON COLUMN gallery_files.file_id IS '파일 ID (외래키) - file_storage 테이블 참조';
COMMENT ON COLUMN gallery_files.sort_order IS '정렬 순서';
COMMENT ON COLUMN gallery_files.created_at IS '생성일시';

CREATE INDEX idx_gallery_files_gallery_sort
  ON gallery_files(gallery_id, sort_order, created_at);

-- ============================================
-- 5. 식단표 (2 Tables)
-- ============================================

-- MEAL_PLANS (식단표)
CREATE TABLE meal_plans (
  id             BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  facility_code  TEXT NOT NULL DEFAULT 'DEFAULT',
  week_start_date DATE NOT NULL,
  meal_month     INT NOT NULL DEFAULT 0,
  created_by     BIGINT REFERENCES employees(id) ON DELETE SET NULL,
  nutrition_manager TEXT,
  status         TEXT NOT NULL DEFAULT 'DRAFT',
  notes          TEXT,
  created_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT meal_plans_uniq_week UNIQUE (facility_code, week_start_date)
);

COMMENT ON TABLE meal_plans IS '주간 식단표 - 시설별 주간 식단 계획 헤더';
COMMENT ON COLUMN meal_plans.id IS '식단표 ID (Primary Key)';
COMMENT ON COLUMN meal_plans.facility_code IS '시설 코드 - 다중 시설 운영 시 구분';
COMMENT ON COLUMN meal_plans.week_start_date IS '주 시작일 (월요일)';
COMMENT ON COLUMN meal_plans.meal_month IS '식단 대상 월 (YYYYMM 형식, 예: 202601)';
COMMENT ON COLUMN meal_plans.created_by IS '작성자 ID (외래키) - employees 테이블 참조';
COMMENT ON COLUMN meal_plans.nutrition_manager IS '영양 관리자';
COMMENT ON COLUMN meal_plans.status IS '상태 - DRAFT: 작성중, PUBLISHED: 게시, ARCHIVED: 보관';
COMMENT ON COLUMN meal_plans.notes IS '비고';
COMMENT ON COLUMN meal_plans.created_at IS '생성일시';
COMMENT ON COLUMN meal_plans.updated_at IS '수정일시';

CREATE INDEX idx_meal_plans_status_week
  ON meal_plans(status, facility_code, week_start_date DESC)
  WHERE status = 'PUBLISHED';

CREATE TRIGGER trg_meal_plans_u
BEFORE UPDATE ON meal_plans
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- MEAL_PLAN_ITEMS (식단 항목)
CREATE TABLE meal_plan_items (
  id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  meal_plan_id  BIGINT NOT NULL REFERENCES meal_plans(id) ON DELETE CASCADE,
  meal_date     DATE NOT NULL,
  meal_type     TEXT NOT NULL,
  menu_content  TEXT NOT NULL,
  image_url     TEXT,
  calories      INT,
  notes         TEXT,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE meal_plan_items IS '식단표 세부 항목 - 요일/식사별 메뉴 구성';
COMMENT ON COLUMN meal_plan_items.id IS '항목 ID (Primary Key)';
COMMENT ON COLUMN meal_plan_items.meal_plan_id IS '식단표 ID (외래키) - meal_plans 테이블 참조';
COMMENT ON COLUMN meal_plan_items.meal_date IS '식사 날짜';
COMMENT ON COLUMN meal_plan_items.meal_type IS '식사 유형 - BREAKFAST: 아침, LUNCH: 점심, DINNER: 저녁, SNACK: 간식';
COMMENT ON COLUMN meal_plan_items.menu_content IS '메뉴 내용';
COMMENT ON COLUMN meal_plan_items.image_url IS '메뉴 이미지 URL';
COMMENT ON COLUMN meal_plan_items.calories IS '칼로리 (kcal)';
COMMENT ON COLUMN meal_plan_items.notes IS '비고';
COMMENT ON COLUMN meal_plan_items.created_at IS '생성일시';
COMMENT ON COLUMN meal_plan_items.updated_at IS '수정일시';

CREATE INDEX idx_meal_plan_items_plan_date
  ON meal_plan_items(meal_plan_id, meal_date, meal_type);

CREATE INDEX idx_meal_plan_items_date_range
  ON meal_plan_items(meal_date, meal_type);

CREATE TRIGGER trg_meal_plan_items_u
BEFORE UPDATE ON meal_plan_items
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- ============================================
-- 6. 프로그램 (3 Tables)
-- ============================================

-- PROGRAMS (프로그램)
CREATE TABLE programs (
  id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  title         TEXT NOT NULL,
  description   TEXT,
  category      TEXT,
  is_active     BOOLEAN NOT NULL DEFAULT TRUE,
  created_by    BIGINT REFERENCES employees(id) ON DELETE SET NULL,
  meta          JSONB NOT NULL DEFAULT '{}'::jsonb,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE programs IS '프로그램 마스터 - 레크리에이션, 치료 프로그램 정의';
COMMENT ON COLUMN programs.id IS '프로그램 ID (Primary Key)';
COMMENT ON COLUMN programs.title IS '프로그램명';
COMMENT ON COLUMN programs.description IS '프로그램 설명';
COMMENT ON COLUMN programs.category IS '카테고리 - EXERCISE: 운동, MUSIC: 음악, COGNITIVE: 인지, ART: 미술, CRAFT: 공예, SPORTS: 스포츠, RECREATION: 레크리에이션, OTHER: 기타';
COMMENT ON COLUMN programs.is_active IS '활성화 여부';
COMMENT ON COLUMN programs.created_by IS '생성자 ID (외래키) - employees 테이블 참조';
COMMENT ON COLUMN programs.meta IS '추가 메타데이터 (JSON)';
COMMENT ON COLUMN programs.created_at IS '생성일시';
COMMENT ON COLUMN programs.updated_at IS '수정일시';

CREATE INDEX idx_programs_active_category
  ON programs(is_active, category)
  WHERE is_active = true;

CREATE TRIGGER trg_programs_u
BEFORE UPDATE ON programs
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- PROGRAM_SCHEDULES (프로그램 일정)
CREATE TABLE program_schedules (
  id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  program_id    BIGINT NOT NULL REFERENCES programs(id) ON DELETE CASCADE,
  starts_at     TIMESTAMPTZ NOT NULL,
  ends_at       TIMESTAMPTZ NOT NULL,
  location      TEXT,
  capacity      INT,
  status        TEXT NOT NULL DEFAULT 'PLANNED',
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT program_schedules_time_ck
    CHECK (ends_at > starts_at)
);

COMMENT ON TABLE program_schedules IS '프로그램 일정 - 프로그램 실행 인스턴스';
COMMENT ON COLUMN program_schedules.id IS '일정 ID (Primary Key)';
COMMENT ON COLUMN program_schedules.program_id IS '프로그램 ID (외래키) - programs 테이블 참조';
COMMENT ON COLUMN program_schedules.starts_at IS '시작일시';
COMMENT ON COLUMN program_schedules.ends_at IS '종료일시';
COMMENT ON COLUMN program_schedules.location IS '장소';
COMMENT ON COLUMN program_schedules.capacity IS '정원 (최대 참가 인원)';
COMMENT ON COLUMN program_schedules.status IS '상태 - PLANNED: 계획, CONFIRMED: 확정, ONGOING: 진행중, DONE: 완료, CANCELLED: 취소';
COMMENT ON COLUMN program_schedules.created_at IS '생성일시';
COMMENT ON COLUMN program_schedules.updated_at IS '수정일시';

CREATE INDEX idx_program_schedules_program_time
  ON program_schedules(program_id, starts_at);

CREATE INDEX idx_program_schedules_upcoming
  ON program_schedules(program_id, starts_at, status);

CREATE TRIGGER trg_program_schedules_u
BEFORE UPDATE ON program_schedules
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- PROGRAM_ATTENDANCE (프로그램 참석)
CREATE TABLE program_attendance (
  id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  schedule_id     BIGINT NOT NULL REFERENCES program_schedules(id) ON DELETE CASCADE,
  resident_id     BIGINT REFERENCES residents(id) ON DELETE CASCADE,
  employee_id     BIGINT REFERENCES employees(id) ON DELETE SET NULL,
  role            TEXT NOT NULL DEFAULT 'PARTICIPANT',
  attended        BOOLEAN NOT NULL DEFAULT FALSE,
  checked_at      TIMESTAMPTZ,
  notes           TEXT,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT program_attendance_uniq
    UNIQUE (schedule_id, resident_id)
);

COMMENT ON TABLE program_attendance IS '프로그램 참석 기록 - 입소자 및 직원의 프로그램 참여 기록';
COMMENT ON COLUMN program_attendance.id IS '참석 ID (Primary Key)';
COMMENT ON COLUMN program_attendance.schedule_id IS '일정 ID (외래키) - program_schedules 테이블 참조';
COMMENT ON COLUMN program_attendance.resident_id IS '입소자 ID (외래키) - 참가자가 입소자인 경우';
COMMENT ON COLUMN program_attendance.employee_id IS '직원 ID (외래키) - 참가자 또는 진행자가 직원인 경우';
COMMENT ON COLUMN program_attendance.role IS '역할 - PARTICIPANT: 참가자, HELPER: 보조자, FACILITATOR: 진행자';
COMMENT ON COLUMN program_attendance.attended IS '참석 여부';
COMMENT ON COLUMN program_attendance.checked_at IS '체크인 일시';
COMMENT ON COLUMN program_attendance.notes IS '비고';
COMMENT ON COLUMN program_attendance.created_at IS '생성일시';

CREATE INDEX idx_program_attendance_schedule
  ON program_attendance(schedule_id, attended);

-- ============================================
-- 7. 면회 및 상담 (2 Tables)
-- ============================================

-- VISIT_RESERVATIONS (면회 예약)
CREATE TABLE visit_reservations (
  id                    BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  visitor_name          TEXT NOT NULL,
  visitor_phone         TEXT NOT NULL,
  visitor_relationship  TEXT NOT NULL,
  resident_name         TEXT NOT NULL,
  visit_date            DATE NOT NULL,
  visit_time            TEXT NOT NULL,
  visitor_count         INT NOT NULL DEFAULT 1,
  visit_purpose         TEXT,
  health_check_symptoms BOOLEAN NOT NULL DEFAULT FALSE,
  health_check_assistance BOOLEAN NOT NULL DEFAULT FALSE,
  notes                 TEXT,
  is_consented          BOOLEAN NOT NULL DEFAULT FALSE,
  status                TEXT NOT NULL DEFAULT 'PENDING',
  ip_address            TEXT,
  user_agent            TEXT,
  created_at            TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at            TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE visit_reservations IS '면회 예약 - 홈페이지 면회 예약 신청 관리';
COMMENT ON COLUMN visit_reservations.id IS '예약 ID (Primary Key)';
COMMENT ON COLUMN visit_reservations.visitor_name IS '방문자 성함';
COMMENT ON COLUMN visit_reservations.visitor_phone IS '방문자 연락처';
COMMENT ON COLUMN visit_reservations.visitor_relationship IS '어르신과의 관계';
COMMENT ON COLUMN visit_reservations.resident_name IS '어르신 성함';
COMMENT ON COLUMN visit_reservations.visit_date IS '방문 희망일';
COMMENT ON COLUMN visit_reservations.visit_time IS '방문 희망시간';
COMMENT ON COLUMN visit_reservations.visitor_count IS '방문 인원';
COMMENT ON COLUMN visit_reservations.visit_purpose IS '방문 목적';
COMMENT ON COLUMN visit_reservations.health_check_symptoms IS '감염 증상 여부 (발열, 기침 등)';
COMMENT ON COLUMN visit_reservations.health_check_assistance IS '이동 보조 필요 여부';
COMMENT ON COLUMN visit_reservations.notes IS '추가 요청사항';
COMMENT ON COLUMN visit_reservations.is_consented IS '개인정보 수집 동의 여부';
COMMENT ON COLUMN visit_reservations.status IS '처리 상태 (PENDING, APPROVED, REJECTED, CANCELLED)';
COMMENT ON COLUMN visit_reservations.ip_address IS '신청 IP 주소';
COMMENT ON COLUMN visit_reservations.user_agent IS '사용자 브라우저 정보';
COMMENT ON COLUMN visit_reservations.created_at IS '신청 시각';
COMMENT ON COLUMN visit_reservations.updated_at IS '수정 시각';

CREATE TRIGGER trg_visit_reservations_u
BEFORE UPDATE ON visit_reservations
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- WEB_INQUIRIES (웹사이트 상담 문의)
CREATE TABLE web_inquiries (
  id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name          TEXT NOT NULL,
  phone         TEXT NOT NULL,
  email         TEXT,
  type          TEXT NOT NULL,
  resident_age  TEXT,
  care_grade    TEXT,
  preferred_date DATE,
  message       TEXT,
  is_consented  BOOLEAN NOT NULL DEFAULT FALSE,
  status        TEXT NOT NULL DEFAULT 'PENDING',
  ip_address    TEXT,
  user_agent    TEXT,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE web_inquiries IS '웹사이트 상담 문의 - 홈페이지 상담 신청 접수용';
COMMENT ON COLUMN web_inquiries.id IS '문의 ID (Primary Key)';
COMMENT ON COLUMN web_inquiries.name IS '상담자 이름';
COMMENT ON COLUMN web_inquiries.phone IS '상담자 연락처';
COMMENT ON COLUMN web_inquiries.email IS '이메일';
COMMENT ON COLUMN web_inquiries.type IS '문의 유형 (입소 상담, 비용 상담 등)';
COMMENT ON COLUMN web_inquiries.resident_age IS '어르신 연령';
COMMENT ON COLUMN web_inquiries.care_grade IS '장기요양등급';
COMMENT ON COLUMN web_inquiries.preferred_date IS '방문 상담 희망일';
COMMENT ON COLUMN web_inquiries.message IS '상세 문의사항';
COMMENT ON COLUMN web_inquiries.is_consented IS '개인정보 수집 동의 여부';
COMMENT ON COLUMN web_inquiries.status IS '처리 상태 (PENDING, IN_PROGRESS, DONE, CANCELLED)';
COMMENT ON COLUMN web_inquiries.ip_address IS '문의 접수 IP 주소';
COMMENT ON COLUMN web_inquiries.user_agent IS '사용자 브라우저 정보';
COMMENT ON COLUMN web_inquiries.created_at IS '문의 생성 시각';
COMMENT ON COLUMN web_inquiries.updated_at IS '수정 시각';

CREATE TRIGGER trg_web_inquiries_u
BEFORE UPDATE ON web_inquiries
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- ============================================
-- 8. 팝업 및 배너 (1 Table)
-- ============================================

-- POPUP_BANNERS (팝업 배너)
CREATE TABLE popup_banners (
  id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  title           TEXT NOT NULL,
  content         TEXT,
  image_url       TEXT,
  link_url        TEXT,
  display_type    TEXT NOT NULL DEFAULT 'POPUP',
  position        TEXT,
  width           INT,
  height          INT,
  start_date      DATE NOT NULL,
  end_date        DATE NOT NULL,
  is_active       BOOLEAN NOT NULL DEFAULT TRUE,
  show_once       BOOLEAN NOT NULL DEFAULT FALSE,
  priority        INT NOT NULL DEFAULT 0,
  created_by      BIGINT REFERENCES employees(id) ON DELETE SET NULL,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE popup_banners IS '팝업 배너 - 홈페이지 팝업 및 모달 배너 관리';
COMMENT ON COLUMN popup_banners.id IS '배너 ID (Primary Key)';
COMMENT ON COLUMN popup_banners.title IS '배너 제목';
COMMENT ON COLUMN popup_banners.content IS '배너 내용';
COMMENT ON COLUMN popup_banners.image_url IS '이미지 URL';
COMMENT ON COLUMN popup_banners.link_url IS '링크 URL';
COMMENT ON COLUMN popup_banners.display_type IS '표시 유형 - POPUP, MODAL, BANNER';
COMMENT ON COLUMN popup_banners.position IS '표시 위치';
COMMENT ON COLUMN popup_banners.width IS '너비 (px)';
COMMENT ON COLUMN popup_banners.height IS '높이 (px)';
COMMENT ON COLUMN popup_banners.start_date IS '게시 시작일';
COMMENT ON COLUMN popup_banners.end_date IS '게시 종료일';
COMMENT ON COLUMN popup_banners.is_active IS '활성화 여부';
COMMENT ON COLUMN popup_banners.show_once IS '하루 동안 보지 않기 옵션 여부';
COMMENT ON COLUMN popup_banners.priority IS '우선순위 (높을수록 먼저 표시)';
COMMENT ON COLUMN popup_banners.created_by IS '작성자 ID (외래키) - employees 테이블 참조';
COMMENT ON COLUMN popup_banners.created_at IS '생성일시';
COMMENT ON COLUMN popup_banners.updated_at IS '수정일시';

CREATE INDEX idx_popup_banners_active_dates
  ON popup_banners(is_active, start_date, end_date)
  WHERE is_active = true;

CREATE INDEX idx_popup_banners_priority
  ON popup_banners(priority DESC);

CREATE TRIGGER trg_popup_banners_u
BEFORE UPDATE ON popup_banners
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- ============================================
-- 9. 생활실 및 배정 관리
-- ============================================

-- ROOMS (생활실)
CREATE TABLE rooms (
  id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  facility_id   BIGINT REFERENCES facilities(id) ON DELETE CASCADE,
  floor         TEXT NOT NULL,
  room_name     TEXT NOT NULL,
  capacity      INT DEFAULT 4,
  is_active     BOOLEAN DEFAULT TRUE,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT rooms_name_uniq UNIQUE(facility_id, floor, room_name)
);

COMMENT ON TABLE rooms IS '생활실 정보 - 층별 생활실 구성 및 정원 관리';
COMMENT ON COLUMN rooms.id IS '생활실 ID (Primary Key)';
COMMENT ON COLUMN rooms.facility_id IS '시설 ID (외래키) - facilities 테이블 참조';
COMMENT ON COLUMN rooms.floor IS '층 (예: 1층, 2층)';
COMMENT ON COLUMN rooms.room_name IS '생활실명 (예: 101호, 민들레방)';
COMMENT ON COLUMN rooms.capacity IS '정원';
COMMENT ON COLUMN rooms.is_active IS '사용 가능 여부';
COMMENT ON COLUMN rooms.created_at IS '생성일시';
COMMENT ON COLUMN rooms.updated_at IS '수정일시';

CREATE TRIGGER trg_rooms_u
BEFORE UPDATE ON rooms
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- RESIDENT_ROOMS (입소자 생활실 배정)
CREATE TABLE resident_rooms (
  id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  resident_id   BIGINT NOT NULL REFERENCES residents(id) ON DELETE CASCADE,
  room_id       BIGINT NOT NULL REFERENCES rooms(id),
  bed_label     TEXT,
  starts_at     DATE NOT NULL,
  ends_at       DATE,
  is_primary    BOOLEAN NOT NULL DEFAULT TRUE,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT resident_rooms_date_ck
    CHECK (ends_at IS NULL OR ends_at > starts_at)
);

COMMENT ON TABLE resident_rooms IS '입소자 생활실 배정 이력';
COMMENT ON COLUMN resident_rooms.id IS '배정 ID (Primary Key)';
COMMENT ON COLUMN resident_rooms.resident_id IS '입소자 ID (외래키) - residents 테이블 참조';
COMMENT ON COLUMN resident_rooms.room_id IS '생활실 ID (외래키) - rooms 테이블 참조';
COMMENT ON COLUMN resident_rooms.bed_label IS '침대 번호/위치 (선택)';
COMMENT ON COLUMN resident_rooms.starts_at IS '배정 시작일';
COMMENT ON COLUMN resident_rooms.ends_at IS '배정 종료일 (NULL이면 현재 배정중)';
COMMENT ON COLUMN resident_rooms.is_primary IS '주 배정 여부';
COMMENT ON COLUMN resident_rooms.created_at IS '생성일시';
COMMENT ON COLUMN resident_rooms.updated_at IS '수정일시';

CREATE INDEX idx_resident_rooms_resident
  ON resident_rooms(resident_id, starts_at);

CREATE INDEX idx_resident_rooms_room_active
  ON resident_rooms(room_id, ends_at)
  WHERE ends_at IS NULL;

CREATE TRIGGER trg_resident_rooms_u
BEFORE UPDATE ON resident_rooms
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- ============================================
-- 10. 알림 및 추가 서비스
-- ============================================

-- NOTIFICATION_TEMPLATES (알림 템플릿)
CREATE TABLE notification_templates (
  id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name        TEXT NOT NULL,
  category    TEXT NOT NULL,
  channel     TEXT NOT NULL,
  content     TEXT NOT NULL,
  variables   JSONB NOT NULL DEFAULT '[]',
  status      TEXT NOT NULL DEFAULT 'ACTIVE'
                CONSTRAINT notif_template_status_ck CHECK (status IN ('ACTIVE','INACTIVE')),
  usage_count INT NOT NULL DEFAULT 0,
  created_by  BIGINT,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE notification_templates IS '알림 템플릿 - 자주 사용하는 알림 메시지 저장';
COMMENT ON COLUMN notification_templates.id IS '템플릿 ID (Primary Key)';
COMMENT ON COLUMN notification_templates.name IS '템플릿 이름';
COMMENT ON COLUMN notification_templates.category IS '카테고리 (예: 입소안내, 투약알림, 긴급)';
COMMENT ON COLUMN notification_templates.channel IS '발송 채널 - SMS | EMAIL | PUSH | KAKAO';
COMMENT ON COLUMN notification_templates.content IS '템플릿 내용';
COMMENT ON COLUMN notification_templates.variables IS '치환 변수 목록 (JSON 배열)';
COMMENT ON COLUMN notification_templates.status IS '상태 - ACTIVE | INACTIVE';
COMMENT ON COLUMN notification_templates.usage_count IS '사용 횟수';
COMMENT ON COLUMN notification_templates.created_by IS '생성자 ID';
COMMENT ON COLUMN notification_templates.created_at IS '생성일시';
COMMENT ON COLUMN notification_templates.updated_at IS '수정일시';

CREATE INDEX idx_notif_template_category ON notification_templates(category);
CREATE INDEX idx_notif_template_channel ON notification_templates(channel);

CREATE TRIGGER trg_notification_templates_u
BEFORE UPDATE ON notification_templates
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- NOTIFICATION_CAMPAIGNS (알림 캠페인 — 즉시·예약 발송 단위)
CREATE TABLE notification_campaigns (
  id                 BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  campaign_name      TEXT NOT NULL,
  purpose            TEXT,
  title              TEXT,
  body               TEXT,
  channel            TEXT NOT NULL,
  send_type          TEXT NOT NULL
                      CONSTRAINT campaign_send_type_ck CHECK (send_type IN ('IMMEDIATE','SCHEDULED')),
  scheduled_at       TIMESTAMPTZ,
  sent_at            TIMESTAMPTZ,
  status             TEXT NOT NULL DEFAULT 'PENDING'
                      CONSTRAINT campaign_status_ck CHECK (status IN ('PENDING','SENT','CANCELLED','FAILED')),
  recipient_count    INT NOT NULL DEFAULT 0,
  payload            JSONB NOT NULL DEFAULT '{}',
  template_id        BIGINT REFERENCES notification_templates(id) ON DELETE SET NULL,
  recipient_group_id BIGINT,
  created_by         BIGINT,
  created_at         TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at         TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE notification_campaigns IS '알림 캠페인 - 즉시 발송 및 예약 발송 관리';
COMMENT ON COLUMN notification_campaigns.id IS '캠페인 ID (Primary Key)';
COMMENT ON COLUMN notification_campaigns.campaign_name IS '캠페인 이름';
COMMENT ON COLUMN notification_campaigns.purpose IS '발송 목적';
COMMENT ON COLUMN notification_campaigns.title IS '알림 제목';
COMMENT ON COLUMN notification_campaigns.body IS '알림 내용';
COMMENT ON COLUMN notification_campaigns.channel IS '발송 채널 - SMS | EMAIL | PUSH | KAKAO';
COMMENT ON COLUMN notification_campaigns.send_type IS '발송 유형 - IMMEDIATE | SCHEDULED';
COMMENT ON COLUMN notification_campaigns.scheduled_at IS '예약 발송 시각';
COMMENT ON COLUMN notification_campaigns.sent_at IS '실제 발송 완료 시각';
COMMENT ON COLUMN notification_campaigns.status IS '상태 - PENDING | SENT | CANCELLED | FAILED';
COMMENT ON COLUMN notification_campaigns.recipient_count IS '수신자 수';
COMMENT ON COLUMN notification_campaigns.payload IS '추가 데이터 (JSON)';
COMMENT ON COLUMN notification_campaigns.template_id IS '연결된 템플릿 ID';
COMMENT ON COLUMN notification_campaigns.recipient_group_id IS '연결된 수신자 그룹 ID';
COMMENT ON COLUMN notification_campaigns.created_at IS '생성일시';
COMMENT ON COLUMN notification_campaigns.updated_at IS '수정일시';

CREATE INDEX idx_campaign_status_sched ON notification_campaigns(status, scheduled_at);
CREATE INDEX idx_campaign_send_type_status ON notification_campaigns(send_type, status);
CREATE INDEX idx_campaign_created ON notification_campaigns(created_at DESC);

CREATE TRIGGER trg_notification_campaigns_u
BEFORE UPDATE ON notification_campaigns
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- NOTIFICATION_CAMPAIGN_RECIPIENTS (캠페인 수신자)
CREATE TABLE notification_campaign_recipients (
  id             BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  campaign_id    BIGINT NOT NULL REFERENCES notification_campaigns(id) ON DELETE CASCADE,
  recipient_type TEXT NOT NULL,
  recipient_id   BIGINT,
  name           TEXT NOT NULL,
  phone          TEXT,
  status         TEXT NOT NULL DEFAULT 'PENDING'
                  CONSTRAINT campaign_recipient_status_ck CHECK (status IN ('PENDING','SENT','FAILED'))
);

COMMENT ON TABLE notification_campaign_recipients IS '캠페인 수신자 - 캠페인별 개별 수신자 및 발송 결과';
COMMENT ON COLUMN notification_campaign_recipients.id IS '수신자 레코드 ID (Primary Key)';
COMMENT ON COLUMN notification_campaign_recipients.campaign_id IS '캠페인 ID (외래키)';
COMMENT ON COLUMN notification_campaign_recipients.recipient_type IS '수신자 타입 - RESIDENT | EMPLOYEE | GUARDIAN';
COMMENT ON COLUMN notification_campaign_recipients.recipient_id IS '수신자 엔티티 ID (선택)';
COMMENT ON COLUMN notification_campaign_recipients.name IS '수신자 이름';
COMMENT ON COLUMN notification_campaign_recipients.phone IS '연락처';
COMMENT ON COLUMN notification_campaign_recipients.status IS '발송 상태 - PENDING | SENT | FAILED';

CREATE INDEX idx_campaign_recipient_campaign ON notification_campaign_recipients(campaign_id);

-- RECIPIENT_GROUPS (수신자 그룹)
CREATE TABLE recipient_groups (
  id           BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name         TEXT NOT NULL,
  description  TEXT,
  type         TEXT NOT NULL
                CONSTRAINT recipient_group_type_ck CHECK (type IN ('RESIDENT','GUARDIAN','STAFF','MIXED')),
  status       TEXT NOT NULL DEFAULT 'ACTIVE'
                CONSTRAINT recipient_group_status_ck CHECK (status IN ('ACTIVE','INACTIVE')),
  member_count INT NOT NULL DEFAULT 0,
  usage_count  INT NOT NULL DEFAULT 0,
  last_used_at TIMESTAMPTZ,
  created_by   BIGINT,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE recipient_groups IS '수신자 그룹 - 자주 사용하는 발송 대상을 그룹으로 관리';
COMMENT ON COLUMN recipient_groups.id IS '그룹 ID (Primary Key)';
COMMENT ON COLUMN recipient_groups.name IS '그룹명';
COMMENT ON COLUMN recipient_groups.description IS '그룹 설명';
COMMENT ON COLUMN recipient_groups.type IS '그룹 유형 - RESIDENT | GUARDIAN | STAFF | MIXED';
COMMENT ON COLUMN recipient_groups.status IS '상태 - ACTIVE | INACTIVE';
COMMENT ON COLUMN recipient_groups.member_count IS '멤버 수';
COMMENT ON COLUMN recipient_groups.usage_count IS '사용 횟수';
COMMENT ON COLUMN recipient_groups.last_used_at IS '마지막 사용 시각';
COMMENT ON COLUMN recipient_groups.created_at IS '생성일시';
COMMENT ON COLUMN recipient_groups.updated_at IS '수정일시';

CREATE INDEX idx_recipient_group_type_status ON recipient_groups(type, status);

CREATE TRIGGER trg_recipient_groups_u
BEFORE UPDATE ON recipient_groups
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

-- RECIPIENT_GROUP_MEMBERS (수신자 그룹 멤버)
CREATE TABLE recipient_group_members (
  id             BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  group_id       BIGINT NOT NULL REFERENCES recipient_groups(id) ON DELETE CASCADE,
  recipient_type TEXT NOT NULL,
  recipient_id   BIGINT,
  name           TEXT NOT NULL,
  phone          TEXT,
  relation       TEXT
);

COMMENT ON TABLE recipient_group_members IS '수신자 그룹 멤버 - 수신자 그룹에 속한 개별 멤버';
COMMENT ON COLUMN recipient_group_members.id IS '멤버 레코드 ID (Primary Key)';
COMMENT ON COLUMN recipient_group_members.group_id IS '소속 그룹 ID (외래키)';
COMMENT ON COLUMN recipient_group_members.recipient_type IS '수신자 타입 - RESIDENT | EMPLOYEE | GUARDIAN';
COMMENT ON COLUMN recipient_group_members.recipient_id IS '수신자 엔티티 ID (선택)';
COMMENT ON COLUMN recipient_group_members.name IS '이름';
COMMENT ON COLUMN recipient_group_members.phone IS '연락처';
COMMENT ON COLUMN recipient_group_members.relation IS '관계 (보호자인 경우: 자녀, 배우자 등)';

CREATE INDEX idx_recipient_group_member_group ON recipient_group_members(group_id);

-- SMS_CREDITS (문자 크레딧 이력)
CREATE TABLE sms_credits (
  id               BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  balance          INT NOT NULL,
  deducted_amount  INT NOT NULL,
  method           TEXT NOT NULL,
  campaign_id      BIGINT,
  description      TEXT,
  created_at       TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE sms_credits IS '문자 크레딧 이력 - 문자 발송 시 차감되는 크레딧 이력 관리';
COMMENT ON COLUMN sms_credits.id IS '이력 ID (Primary Key)';
COMMENT ON COLUMN sms_credits.balance IS '차감 후 잔액';
COMMENT ON COLUMN sms_credits.deducted_amount IS '차감량 (음수이면 충전)';
COMMENT ON COLUMN sms_credits.method IS '발송 방식 - SMS | LMS | MMS | KAKAO';
COMMENT ON COLUMN sms_credits.campaign_id IS '관련 캠페인 ID (선택)';
COMMENT ON COLUMN sms_credits.description IS '비고';
COMMENT ON COLUMN sms_credits.created_at IS '이력 생성일시';

CREATE INDEX idx_sms_credit_created ON sms_credits(created_at DESC);

-- FACILITY_FILES (시설 자료실)
CREATE TABLE facility_files (
  id           BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name         TEXT NOT NULL,
  category     TEXT NOT NULL,
  file_url     TEXT NOT NULL,
  file_type    TEXT NOT NULL,
  size         BIGINT NOT NULL,
  uploaded_by  TEXT NOT NULL,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE facility_files IS '시설 자료실 - 운영 서식, 케어플랜, 교육자료 등 파일 관리';
COMMENT ON COLUMN facility_files.id IS '파일 ID (Primary Key)';
COMMENT ON COLUMN facility_files.name IS '파일명';
COMMENT ON COLUMN facility_files.category IS '카테고리 - 운영서식 | 케어플랜 | 행정서류 | 교육자료';
COMMENT ON COLUMN facility_files.file_url IS '파일 URL (S3 또는 로컬 경로)';
COMMENT ON COLUMN facility_files.file_type IS '파일 타입 - pdf | xlsx | docx | png | zip';
COMMENT ON COLUMN facility_files.size IS '파일 크기 (bytes)';
COMMENT ON COLUMN facility_files.uploaded_by IS '업로더 (이름 또는 ID)';
COMMENT ON COLUMN facility_files.created_at IS '업로드 일시';

CREATE INDEX idx_facility_file_category ON facility_files(category);
CREATE INDEX idx_facility_file_created ON facility_files(created_at DESC);

-- ============================================
-- 11. 입소자 관리 (Resident Care)
-- ============================================

-- RESIDENT_CONTACTS (입소자 연락처/보호자)
CREATE TABLE resident_contacts (
  id             BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  resident_id    BIGINT NOT NULL REFERENCES residents(id) ON DELETE CASCADE ON UPDATE NO ACTION,
  name           TEXT NOT NULL,
  relationship   TEXT,
  phone_number   TEXT,
  email          TEXT,
  address        TEXT,
  is_primary     BOOLEAN NOT NULL DEFAULT FALSE,
  receive_notice BOOLEAN NOT NULL DEFAULT FALSE,
  created_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at     TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE resident_contacts IS '입소자 연락처 - 보호자 및 비상연락처 정보';
COMMENT ON COLUMN resident_contacts.id IS '연락처 ID (Primary Key)';
COMMENT ON COLUMN resident_contacts.resident_id IS '입소자 ID (외래키)';
COMMENT ON COLUMN resident_contacts.name IS '연락처 이름';
COMMENT ON COLUMN resident_contacts.relationship IS '관계 (자녀, 배우자 등)';
COMMENT ON COLUMN resident_contacts.phone_number IS '전화번호';
COMMENT ON COLUMN resident_contacts.email IS '이메일';
COMMENT ON COLUMN resident_contacts.address IS '주소';
COMMENT ON COLUMN resident_contacts.is_primary IS '주 연락처 여부';
COMMENT ON COLUMN resident_contacts.receive_notice IS '알림 수신 여부';

CREATE TRIGGER trg_resident_contacts_u
BEFORE UPDATE ON resident_contacts
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE INDEX idx_resident_contacts_resident ON resident_contacts(resident_id);


-- RESIDENT_MEDICATIONS (입소자 투약 정보)
CREATE TABLE resident_medications (
  id             BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  resident_id    BIGINT NOT NULL REFERENCES residents(id) ON DELETE CASCADE ON UPDATE NO ACTION,
  prescribed_by  TEXT,
  drug_name      TEXT NOT NULL,
  dosage         TEXT NOT NULL,
  schedule       TEXT,
  start_date     DATE NOT NULL,
  end_date       DATE,
  notes          TEXT,
  created_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at     TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE resident_medications IS '입소자 투약 정보 - 처방약 및 복용 일정 관리';
COMMENT ON COLUMN resident_medications.id IS '투약 ID (Primary Key)';
COMMENT ON COLUMN resident_medications.resident_id IS '입소자 ID (외래키)';
COMMENT ON COLUMN resident_medications.prescribed_by IS '처방의';
COMMENT ON COLUMN resident_medications.drug_name IS '약물명';
COMMENT ON COLUMN resident_medications.dosage IS '용량';
COMMENT ON COLUMN resident_medications.schedule IS '복용 일정';
COMMENT ON COLUMN resident_medications.start_date IS '투약 시작일';
COMMENT ON COLUMN resident_medications.end_date IS '투약 종료일';
COMMENT ON COLUMN resident_medications.notes IS '비고';

CREATE TRIGGER trg_resident_medications_u
BEFORE UPDATE ON resident_medications
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE INDEX idx_resident_medications_resident ON resident_medications(resident_id);
CREATE INDEX idx_resident_medications_active ON resident_medications(resident_id, end_date);


-- RESIDENT_CONTRACTS (입소자 표준약관/계약서)
CREATE TABLE resident_contracts (
  id             BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  resident_id    BIGINT NOT NULL REFERENCES residents(id) ON DELETE CASCADE ON UPDATE NO ACTION,
  created_by     BIGINT REFERENCES employees(id) ON DELETE SET NULL ON UPDATE NO ACTION,
  contract_date  DATE NOT NULL,
  contract_type  TEXT NOT NULL DEFAULT 'STANDARD'
                   CONSTRAINT resident_contract_type_ck CHECK (contract_type IN ('STANDARD','LONG_TERM','SHORT_TERM')),
  signed_by      TEXT,
  agreed_at      TIMESTAMPTZ,
  status         TEXT NOT NULL DEFAULT 'ACTIVE'
                   CONSTRAINT resident_contract_status_ck CHECK (status IN ('ACTIVE','EXPIRED','CANCELLED')),
  content        TEXT,
  file_id        BIGINT REFERENCES file_storage(id) ON DELETE SET NULL ON UPDATE NO ACTION,
  created_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at     TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE resident_contracts IS '입소자 계약서 - 표준약관 및 계약 정보 관리';
COMMENT ON COLUMN resident_contracts.id IS '계약 ID (Primary Key)';
COMMENT ON COLUMN resident_contracts.resident_id IS '입소자 ID (외래키)';
COMMENT ON COLUMN resident_contracts.created_by IS '작성자 ID (외래키)';
COMMENT ON COLUMN resident_contracts.contract_date IS '계약일';
COMMENT ON COLUMN resident_contracts.contract_type IS '계약 유형 (STANDARD|LONG_TERM|SHORT_TERM)';
COMMENT ON COLUMN resident_contracts.signed_by IS '서명자명';
COMMENT ON COLUMN resident_contracts.agreed_at IS '동의 일시';
COMMENT ON COLUMN resident_contracts.status IS '상태 (ACTIVE|EXPIRED|CANCELLED)';
COMMENT ON COLUMN resident_contracts.content IS '계약 내용 (전문)';
COMMENT ON COLUMN resident_contracts.file_id IS '첨부 파일 ID';

CREATE TRIGGER trg_resident_contracts_u
BEFORE UPDATE ON resident_contracts
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE INDEX idx_resident_contracts_resident ON resident_contracts(resident_id, status);


-- RESIDENT_ASSESSMENTS (입소자 기초평가)
CREATE TABLE resident_assessments (
  id               BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  resident_id      BIGINT NOT NULL REFERENCES residents(id) ON DELETE CASCADE ON UPDATE NO ACTION,
  assessed_by      BIGINT REFERENCES employees(id) ON DELETE SET NULL ON UPDATE NO ACTION,
  assessment_type  TEXT NOT NULL
                     CONSTRAINT resident_assessment_type_ck CHECK (assessment_type IN ('FALL_RISK','BEDSORE','COGNITIVE','NEEDS')),
  assessment_round INT NOT NULL DEFAULT 1,
  reason           TEXT NOT NULL DEFAULT 'REASSESSMENT'
                     CONSTRAINT resident_assessment_reason_ck CHECK (reason IN ('INITIAL','REASSESSMENT','CONDITION_CHANGE')),
  assessed_at      DATE NOT NULL,
  scores           JSONB NOT NULL DEFAULT '{}',
  total_score      INT,
  risk_level       TEXT,
  notes            TEXT,
  created_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at       TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE resident_assessments IS '입소자 기초평가 - 낙상위험도(Huhn), 욕창위험도(Braden), 인지기능 등';
COMMENT ON COLUMN resident_assessments.id IS '평가 ID (Primary Key)';
COMMENT ON COLUMN resident_assessments.resident_id IS '입소자 ID (외래키)';
COMMENT ON COLUMN resident_assessments.assessed_by IS '평가자 ID (외래키)';
COMMENT ON COLUMN resident_assessments.assessment_type IS '평가 유형 (FALL_RISK|BEDSORE|COGNITIVE|NEEDS)';
COMMENT ON COLUMN resident_assessments.assessment_round IS '평가 회차';
COMMENT ON COLUMN resident_assessments.reason IS '조사 사유 (INITIAL|REASSESSMENT|CONDITION_CHANGE)';
COMMENT ON COLUMN resident_assessments.assessed_at IS '평가일';
COMMENT ON COLUMN resident_assessments.scores IS '항목별 점수 JSON (낙상: {age,mental,elimination,fall_history,activity,gait,medication}, 욕창: {sensory,moisture,activity,mobility,nutrition,friction})';
COMMENT ON COLUMN resident_assessments.total_score IS '합계 점수';
COMMENT ON COLUMN resident_assessments.risk_level IS '위험 등급';
COMMENT ON COLUMN resident_assessments.notes IS '비고';

CREATE TRIGGER trg_resident_assessments_u
BEFORE UPDATE ON resident_assessments
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE INDEX idx_resident_assessments_resident_type ON resident_assessments(resident_id, assessment_type, assessed_at DESC);
CREATE INDEX idx_resident_assessments_type ON resident_assessments(assessment_type);


-- RESIDENT_EXTRA_COSTS (입소자 비급여/기타 항목)
CREATE TABLE resident_extra_costs (
  id           BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  resident_id  BIGINT NOT NULL REFERENCES residents(id) ON DELETE CASCADE ON UPDATE NO ACTION,
  created_by   BIGINT REFERENCES employees(id) ON DELETE SET NULL ON UPDATE NO ACTION,
  year_month   TEXT NOT NULL,
  item_name    TEXT NOT NULL,
  unit_price   INT NOT NULL,
  quantity     INT NOT NULL DEFAULT 1,
  total_amount INT NOT NULL,
  occurred_at  DATE NOT NULL,
  notes        TEXT,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE resident_extra_costs IS '입소자 비급여/기타 - 식재료비 등 비급여 항목 청구 관리';
COMMENT ON COLUMN resident_extra_costs.id IS '비급여 항목 ID (Primary Key)';
COMMENT ON COLUMN resident_extra_costs.resident_id IS '입소자 ID (외래키)';
COMMENT ON COLUMN resident_extra_costs.created_by IS '등록자 ID (외래키)';
COMMENT ON COLUMN resident_extra_costs.year_month IS '해당 연월 (예: 2026-02)';
COMMENT ON COLUMN resident_extra_costs.item_name IS '항목명';
COMMENT ON COLUMN resident_extra_costs.unit_price IS '단가';
COMMENT ON COLUMN resident_extra_costs.quantity IS '횟수/수량';
COMMENT ON COLUMN resident_extra_costs.total_amount IS '총 금액';
COMMENT ON COLUMN resident_extra_costs.occurred_at IS '발생일자';
COMMENT ON COLUMN resident_extra_costs.notes IS '비고';

CREATE TRIGGER trg_resident_extra_costs_u
BEFORE UPDATE ON resident_extra_costs
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE INDEX idx_resident_extra_costs_resident_month ON resident_extra_costs(resident_id, year_month);
CREATE INDEX idx_resident_extra_costs_month ON resident_extra_costs(year_month);


-- RESIDENT_PAYMENTS (입소자 본인부담금)
CREATE TABLE resident_payments (
  id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  resident_id     BIGINT NOT NULL REFERENCES residents(id) ON DELETE CASCADE ON UPDATE NO ACTION,
  payment_month   TEXT NOT NULL,
  claim_amount    INT NOT NULL DEFAULT 0,
  paid_amount     INT NOT NULL DEFAULT 0,
  unpaid_amount   INT NOT NULL DEFAULT 0,
  deposit_date    DATE,
  depositor_name  TEXT,
  payment_method  TEXT
                    CONSTRAINT resident_payment_method_ck CHECK (payment_method IN ('BANK_TRANSFER','CASH','CARD') OR payment_method IS NULL),
  receipt_no      TEXT,
  notes           TEXT,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE resident_payments IS '입소자 본인부담금 - 청구이력 및 입금내역 관리';
COMMENT ON COLUMN resident_payments.id IS '납부 ID (Primary Key)';
COMMENT ON COLUMN resident_payments.resident_id IS '입소자 ID (외래키)';
COMMENT ON COLUMN resident_payments.payment_month IS '급여제공월 (예: 2026-01)';
COMMENT ON COLUMN resident_payments.claim_amount IS '청구금액';
COMMENT ON COLUMN resident_payments.paid_amount IS '입금액';
COMMENT ON COLUMN resident_payments.unpaid_amount IS '미납액';
COMMENT ON COLUMN resident_payments.deposit_date IS '입금일';
COMMENT ON COLUMN resident_payments.depositor_name IS '입금자명';
COMMENT ON COLUMN resident_payments.payment_method IS '결제 방법 (BANK_TRANSFER|CASH|CARD)';
COMMENT ON COLUMN resident_payments.receipt_no IS '영수증 번호';
COMMENT ON COLUMN resident_payments.notes IS '비고';

CREATE TRIGGER trg_resident_payments_u
BEFORE UPDATE ON resident_payments
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE INDEX idx_resident_payments_resident_month ON resident_payments(resident_id, payment_month DESC);


-- RESIDENT_DOCUMENTS (입소자 서류관리)
CREATE TABLE resident_documents (
  id                BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  resident_id       BIGINT NOT NULL REFERENCES residents(id) ON DELETE CASCADE ON UPDATE NO ACTION,
  uploaded_by       BIGINT REFERENCES employees(id) ON DELETE SET NULL ON UPDATE NO ACTION,
  document_name     TEXT NOT NULL,
  document_category TEXT NOT NULL DEFAULT 'OTHER'
                      CONSTRAINT resident_document_category_ck CHECK (document_category IN ('REQUIRED','ID','HEALTH','OTHER')),
  file_id           BIGINT REFERENCES file_storage(id) ON DELETE SET NULL ON UPDATE NO ACTION,
  status            TEXT NOT NULL DEFAULT 'SUBMITTED'
                      CONSTRAINT resident_document_status_ck CHECK (status IN ('SUBMITTED','PENDING','EXPIRED')),
  uploaded_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
  expires_at        DATE,
  notes             TEXT,
  created_at        TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at        TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE resident_documents IS '입소자 서류관리 - 필수서류, 신분증명, 건강서류 등 파일 관리';
COMMENT ON COLUMN resident_documents.id IS '서류 ID (Primary Key)';
COMMENT ON COLUMN resident_documents.resident_id IS '입소자 ID (외래키)';
COMMENT ON COLUMN resident_documents.uploaded_by IS '업로드자 ID (외래키)';
COMMENT ON COLUMN resident_documents.document_name IS '서류명';
COMMENT ON COLUMN resident_documents.document_category IS '서류 카테고리 (REQUIRED|ID|HEALTH|OTHER)';
COMMENT ON COLUMN resident_documents.file_id IS '파일 ID';
COMMENT ON COLUMN resident_documents.status IS '상태 (SUBMITTED|PENDING|EXPIRED)';
COMMENT ON COLUMN resident_documents.uploaded_at IS '업로드 일시';
COMMENT ON COLUMN resident_documents.expires_at IS '유효기간 만료일';
COMMENT ON COLUMN resident_documents.notes IS '비고';

CREATE TRIGGER trg_resident_documents_u
BEFORE UPDATE ON resident_documents
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE INDEX idx_resident_documents_resident_category ON resident_documents(resident_id, document_category);
CREATE INDEX idx_resident_documents_status ON resident_documents(status);


-- CONSULTATION_RECORDS (상담일지)
CREATE TABLE consultation_records (
  id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  resident_id     BIGINT NOT NULL REFERENCES residents(id) ON DELETE CASCADE ON UPDATE NO ACTION,
  counselor_id    BIGINT REFERENCES employees(id) ON DELETE SET NULL ON UPDATE NO ACTION,
  consulted_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  type            TEXT NOT NULL DEFAULT 'GENERAL',
  channel         TEXT,
  summary         TEXT NOT NULL,
  details         TEXT,
  follow_up_date  DATE,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE consultation_records IS '상담일지 - 입소자 및 보호자 상담 기록';
COMMENT ON COLUMN consultation_records.id IS '상담 ID (Primary Key)';
COMMENT ON COLUMN consultation_records.resident_id IS '입소자 ID (외래키)';
COMMENT ON COLUMN consultation_records.counselor_id IS '상담자 ID (외래키)';
COMMENT ON COLUMN consultation_records.consulted_at IS '상담일시';
COMMENT ON COLUMN consultation_records.type IS '상담 유형 (GENERAL, FAMILY, MEDICAL 등)';
COMMENT ON COLUMN consultation_records.channel IS '상담 채널 (전화, 대면, 온라인 등)';
COMMENT ON COLUMN consultation_records.summary IS '상담 요약';
COMMENT ON COLUMN consultation_records.details IS '상담 상세 내용';
COMMENT ON COLUMN consultation_records.follow_up_date IS '후속 조치 예정일';

CREATE INDEX idx_consultation_records_resident ON consultation_records(resident_id, consulted_at DESC);
CREATE INDEX idx_consultation_records_counselor ON consultation_records(counselor_id);


-- CONSULTATION_FILES (상담 첨부파일)
CREATE TABLE consultation_files (
  id               BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  consultation_id  BIGINT NOT NULL REFERENCES consultation_records(id) ON DELETE CASCADE ON UPDATE NO ACTION,
  file_id          BIGINT NOT NULL REFERENCES file_storage(id) ON DELETE CASCADE ON UPDATE NO ACTION,
  created_at       TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE consultation_files IS '상담 첨부파일 - 상담일지와 파일의 연결 테이블';
COMMENT ON COLUMN consultation_files.id IS '연결 ID (Primary Key)';
COMMENT ON COLUMN consultation_files.consultation_id IS '상담 ID (외래키)';
COMMENT ON COLUMN consultation_files.file_id IS '파일 ID (외래키)';

CREATE INDEX idx_consultation_files_consultation ON consultation_files(consultation_id);


-- CARE_PLANS (케어 플랜)
CREATE TABLE care_plans (
  id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  resident_id   BIGINT NOT NULL REFERENCES residents(id) ON DELETE CASCADE ON UPDATE NO ACTION,
  created_by    BIGINT REFERENCES employees(id) ON DELETE SET NULL ON UPDATE NO ACTION,
  title         TEXT NOT NULL,
  goal_summary  TEXT,
  start_date    DATE NOT NULL,
  end_date      DATE,
  status        TEXT NOT NULL DEFAULT 'ACTIVE'
                  CONSTRAINT care_plan_status_ck CHECK (status IN ('ACTIVE','COMPLETED','CANCELLED')),
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE care_plans IS '케어 플랜 - 입소자별 돌봄 계획';
COMMENT ON COLUMN care_plans.id IS '케어 플랜 ID (Primary Key)';
COMMENT ON COLUMN care_plans.resident_id IS '입소자 ID (외래키)';
COMMENT ON COLUMN care_plans.created_by IS '생성자 ID (외래키)';
COMMENT ON COLUMN care_plans.title IS '케어 플랜 제목';
COMMENT ON COLUMN care_plans.goal_summary IS '목표 요약';
COMMENT ON COLUMN care_plans.start_date IS '시작일';
COMMENT ON COLUMN care_plans.end_date IS '종료일';
COMMENT ON COLUMN care_plans.status IS '상태 (ACTIVE|COMPLETED|CANCELLED)';

CREATE TRIGGER trg_care_plans_u
BEFORE UPDATE ON care_plans
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE INDEX idx_care_plans_resident ON care_plans(resident_id, status);


-- CARE_PLAN_ITEMS (케어 플랜 세부 항목)
CREATE TABLE care_plan_items (
  id           BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  care_plan_id BIGINT NOT NULL REFERENCES care_plans(id) ON DELETE CASCADE ON UPDATE NO ACTION,
  sequence_no  INT NOT NULL,
  description  TEXT NOT NULL,
  frequency    TEXT,
  notes        TEXT,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT care_plan_items_seq_uniq UNIQUE (care_plan_id, sequence_no)
);

COMMENT ON TABLE care_plan_items IS '케어 플랜 세부 항목 - 케어 플랜의 개별 돌봄 항목';
COMMENT ON COLUMN care_plan_items.id IS '항목 ID (Primary Key)';
COMMENT ON COLUMN care_plan_items.care_plan_id IS '케어 플랜 ID (외래키)';
COMMENT ON COLUMN care_plan_items.sequence_no IS '순서 번호';
COMMENT ON COLUMN care_plan_items.description IS '항목 설명';
COMMENT ON COLUMN care_plan_items.frequency IS '실시 빈도';
COMMENT ON COLUMN care_plan_items.notes IS '비고';

CREATE TRIGGER trg_care_plan_items_u
BEFORE UPDATE ON care_plan_items
FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE INDEX idx_care_plan_items_plan ON care_plan_items(care_plan_id);


-- ============================================
-- 완료 메시지
-- ============================================

DO $$
BEGIN
  RAISE NOTICE '========================================';
  RAISE NOTICE 'Web 알림마당 DDL 실행 완료';
  RAISE NOTICE '========================================';
  RAISE NOTICE '';
  RAISE NOTICE '생성된 테이블:';
  RAISE NOTICE '  기본: 7 테이블 (site_infos, facilities, departments, employee_roles, employee_permissions, employees, file_storage)';
  RAISE NOTICE '  입소자 & 생활실: 3 테이블 (residents, rooms, resident_rooms)';
  RAISE NOTICE '  공지사항: 2 테이블 (notices, notice_files)';
  RAISE NOTICE '  게시판: 3 테이블 (board_posts, board_files, board_comments)';
  RAISE NOTICE '  갤러리: 2 테이블 (gallery_items, gallery_files)';
  RAISE NOTICE '  식단표: 2 테이블 (meal_plans, meal_plan_items)';
  RAISE NOTICE '  프로그램: 3 테이블 (programs, program_schedules, program_attendance)';
  RAISE NOTICE '  면회/문의: 2 테이블 (visit_reservations, web_inquiries)';
  RAISE NOTICE '  팝업/배너: 1 테이블 (popup_banners)';
  RAISE NOTICE '  알림/추가서비스: 7 테이블 (notification_templates, notification_campaigns, notification_campaign_recipients, recipient_groups, recipient_group_members, sms_credits, facility_files)';
  RAISE NOTICE '';
  RAISE NOTICE '총 32개 테이블';
  RAISE NOTICE '';
  RAISE NOTICE '다음 단계:';
  RAISE NOTICE '  1. 30-web-views.sql 실행 (Web View 생성)';
  RAISE NOTICE '  2. 31-admin.sql 실행 (Admin 추가 테이블)';
  RAISE NOTICE '  3. 40-web-seed.sql 실행 (Web 샘플 데이터)';
  RAISE NOTICE '  4. 41-admin-seed.sql 실행 (Admin 샘플 데이터)';
  RAISE NOTICE '  5. 99-final-setup.sql 실행 (최종 설정)';
  RAISE NOTICE '========================================';
END$$;
