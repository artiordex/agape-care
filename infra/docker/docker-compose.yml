# Description : docker-compose.yml - 📌 Agape-Care Docker Compose 환경
# Author : Shiwoo Min
# Updated : 2026-01-22
# Build & Run Infra Only:
# docker compose -p agape-care --env-file .env -f infra/docker/docker-compose.yml up -d postgres redis redisinsight prometheus grafana n8n
# Full Build:
# docker compose -p agape-care --env-file .env -f infra/docker/docker-compose.yml up -d

services:
  # Next.js Web Frontend
  web:
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile.web
    container_name: agape-care-web
    env_file:
      - ../../.env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=${WEB_PORT:-3000}
      - HOST=0.0.0.0
      # URL
      - NEXT_PUBLIC_API_URL=${API_URL:-http://localhost:8000}
      - NEXT_PUBLIC_WEB_URL=${WEB_URL:-http://localhost:3000}
      # Feature Flags
      - NEXT_PUBLIC_FEATURE_AI_RECOMMENDATIONS=${FEATURE_AI_RECOMMENDATIONS:-true}
      - NEXT_PUBLIC_FEATURE_PAYMENT_GATEWAY=${FEATURE_PAYMENT_GATEWAY:-true}
    expose:
      - '3000'
    ports:
      - '0.0.0.0:${WEB_PORT:-3000}:3000'
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'node -e "fetch(''http://localhost:3000'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 10s
      timeout: 3s
      retries: 10
    networks:
      - agape-care-network

  # Nest.js API Backend
  api:
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile.api
    container_name: agape-care-api
    env_file:
      - ../../.env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=${API_PORT:-8000}
      - HOST=0.0.0.0
      # DB / Redis (container override)
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/${REDIS_DB:-0}
    expose:
      - '8000'
    ports:
      - '0.0.0.0:${API_PORT:-8000}:8000'
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'node -e "fetch(''http://localhost:8000/health'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 10s
      timeout: 3s
      retries: 10
    networks:
      - agape-care-network

  # Admin Dashboard (Next.js)
  admin:
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile.admin
    container_name: agape-care-admin
    env_file:
      - ../../.env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=${ADMIN_PORT:-3001}
      - HOST=0.0.0.0
      - NEXT_PUBLIC_API_URL=${API_URL:-http://localhost:8000}
      - NEXT_PUBLIC_ADMIN_URL=${ADMIN_URL:-http://localhost:3001}
      # Feature Flags
      - NEXT_PUBLIC_FEATURE_ADVANCED_ANALYTICS=${FEATURE_ADVANCED_ANALYTICS:-false}
    expose:
      - '3001'
    ports:
      - '0.0.0.0:${ADMIN_PORT:-3001}:3001'
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'node -e "fetch(''http://localhost:3001'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 10s
      timeout: 3s
      retries: 10
    networks:
      - agape-care-network

  # BullMQ Worker
  worker:
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile.worker
    container_name: agape-care-worker
    restart: unless-stopped
    env_file:
      - ../../.env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - QUEUE_REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/${QUEUE_REDIS_DB:-1}
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'node -e "import(''redis'').then(r=>r.createClient({url:''redis://:${REDIS_PASSWORD}@redis:6379/${QUEUE_REDIS_DB:-1}''}).connect().then(()=>process.exit(0)).catch(()=>process.exit(1)))"',
        ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - agape-care-network

  # PostgreSQL
  postgres:
    image: postgres:17
    container_name: agape-care-postgres
    restart: unless-stopped
    env_file:
      - ../../.env
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - TZ=${TZ}
      - PGDATA=/var/lib/postgresql/data/pgdata
    ports:
      - '0.0.0.0:5432:5432'
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ../database/conf/postgresql.conf:/etc/postgresql/postgresql.conf:ro
      - ../database/conf/pg_hba.conf:/etc/postgresql/pg_hba.conf:ro
      - ../database/init:/docker-entrypoint-initdb.d:ro
    command: >
      -c config_file=/etc/postgresql/postgresql.conf
      -c hba_file=/etc/postgresql/pg_hba.conf
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U $${POSTGRES_USER} -d postgres -h 127.0.0.1 -p 5432']
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - agape-care-network

  # Redis
  redis:
    image: redis:7-alpine
    container_name: agape-care-redis
    env_file:
      - ../../.env
    ports:
      - '0.0.0.0:6379:6379'
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - REDIS_MAXMEMORY=${REDIS_MAXMEMORY:-512mb}
      - REDIS_EVICTION=${REDIS_EVICTION:-allkeys-lru}
    command: >
      sh -c 'exec redis-server
        --appendonly yes
        --appendfsync everysec
        --requirepass "$${REDIS_PASSWORD}"
        --maxmemory "$${REDIS_MAXMEMORY}"
        --maxmemory-policy "$${REDIS_EVICTION}"'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD-SHELL', 'redis-cli -a $${REDIS_PASSWORD} ping | grep PONG']
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - agape-care-network

  # RedisInsight
  redisinsight:
    image: redis/redisinsight:latest
    container_name: agape-care-redisinsight
    ports:
      - '0.0.0.0:${REDISINSIGHT_PORT:-5540}:5540'
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - redisinsight:/data
    networks:
      - agape-care-network

  # n8n Automation
  n8n:
    image: n8nio/n8n:latest
    container_name: agape-care-n8n
    restart: unless-stopped
    env_file:
      - ../../.env
    environment:
      - N8N_HOST=localhost
      - N8N_PORT=${N8N_PORT:-5678}
      - N8N_PROTOCOL=http
      - NODE_ENV=production
      - WEBHOOK_URL=http://localhost:${N8N_PORT:-5678}/
      - GENERIC_TIMEZONE=${TZ:-Asia/Seoul}
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=postgres
      - DB_POSTGRESDB_PORT=5432
      - DB_POSTGRESDB_DATABASE=${N8N_DB_NAME:-n8n}
      - DB_POSTGRESDB_USER=${POSTGRES_USER}
      - DB_POSTGRESDB_PASSWORD=${POSTGRES_PASSWORD}
      - N8N_BASIC_AUTH_ACTIVE=${N8N_BASIC_AUTH_ACTIVE:-true}
      - N8N_BASIC_AUTH_USER=${N8N_BASIC_AUTH_USER:-admin}
      - N8N_BASIC_AUTH_PASSWORD=${N8N_BASIC_AUTH_PASSWORD}
      - N8N_ENCRYPTION_KEY=${N8N_ENCRYPTION_KEY}
      - EXECUTIONS_PROCESS=own
      - EXECUTIONS_MODE=regular
      - EXECUTIONS_TIMEOUT=${N8N_EXECUTIONS_TIMEOUT:-3600}
      - EXECUTIONS_MAX_TIMEOUT=${N8N_EXECUTIONS_MAX_TIMEOUT:-14400}
      - N8N_LOG_LEVEL=info
      - N8N_LOG_OUTPUT=console
      - QUEUE_BULL_REDIS_HOST=redis
      - QUEUE_BULL_REDIS_PORT=6379
      - QUEUE_BULL_REDIS_PASSWORD=${REDIS_PASSWORD}
      - N8N_DEFAULT_BINARY_DATA_MODE=filesystem
      - N8N_BINARY_DATA_STORAGE_PATH=/home/node/.n8n/binaryData
    ports:
      - '0.0.0.0:${N8N_PORT:-5678}:5678'
    volumes:
      - n8n_data:/home/node/.n8n
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost:5678/']
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - agape-care-network

  # Nginx Reverse Proxy
  nginx:
    image: nginx:1.27-alpine
    container_name: agape-care-nginx
    ports:
      - '0.0.0.0:${NGINX_PORT:-80}:80'
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      web:
        condition: service_healthy
      api:
        condition: service_healthy
      admin:
        condition: service_healthy
    networks:
      - agape-care-network

volumes:
  pg_data:
  redis_data:
  redisinsight:
  n8n_data:

networks:
  agape-care-network:
    driver: bridge
