{
  "name": "@agape-care/database",
  "sourceRoot": "packages/database/src",
  "projectType": "library",
  "tags": ["type:lib", "layer:backend"],

  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec node -e \"const fs=require('fs');fs.rmSync('dist/packages/database',{recursive:true,force:true});fs.mkdirSync('dist/packages/database/prisma',{recursive:true});\" && pnpm exec tsc -p packages/database/tsconfig.json --outDir dist/packages/database --declarationDir dist/packages/database --tsBuildInfoFile dist/packages/database/.tsbuildinfo && pnpm exec node -e \"const fs=require('fs');fs.copyFileSync('packages/database/package.json','dist/packages/database/package.json');fs.copyFileSync('packages/database/prisma/schema.prisma','dist/packages/database/prisma/schema.prisma')\""
      },
      "outputs": ["{workspaceRoot}/dist/packages/database"]
    },

    "prisma:generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run generate",
        "cwd": "packages/database"
      }
    },

    "prisma:migrate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run migrate",
        "cwd": "packages/database"
      }
    },

    "prisma:migrate:deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run migrate:deploy",
        "cwd": "packages/database"
      }
    },

    "prisma:studio": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run studio",
        "cwd": "packages/database"
      }
    },

    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["packages/database/**/*.ts"]
      }
    },

    "test": {
      "executor": "@nx/vite:test",
      "options": {
        "passWithNoTests": true
      }
    }
  },

  "implicitDependencies": ["logger"]
}
