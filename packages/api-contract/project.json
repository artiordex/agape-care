{
  "name": "@agape-care/api-contract",
  "sourceRoot": "packages/api-contract/src",
  "projectType": "library",
  "tags": ["type:lib", "layer:shared"],

  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec node -e \"const fs=require('fs');fs.rmSync('dist/packages/api-contract',{recursive:true,force:true});fs.mkdirSync('dist/packages/api-contract',{recursive:true});\" && pnpm exec tsc -p packages/api-contract/tsconfig.json --outDir dist/packages/api-contract --declarationDir dist/packages/api-contract --tsBuildInfoFile dist/packages/api-contract/.tsbuildinfo && pnpm exec node -e \"require('fs').copyFileSync('packages/api-contract/package.json','dist/packages/api-contract/package.json')\""
      },
      "outputs": ["{workspaceRoot}/dist/packages/api-contract"]
    },

    "gen:openapi": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @agape-care/api-contract gen:openapi"
      }
    },

    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["packages/api-contract/**/*.ts"]
      }
    },

    "test": {
      "executor": "@nx/vite:test",
      "options": {
        "passWithNoTests": true
      }
    }
  }
}
