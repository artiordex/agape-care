# Description : ci.yml - 📌 Agape-Care CI 파이프라인
# Author : Shiwoo Min
# Date : 2026-01-22
# - Agape-Care monorepo 구조(apps/*, packages/*) 대응
# - pnpm + Vitest 기반 테스트
# - Nx 선택 사용 가능, 없어도 동작하도록 구성

name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  NODE_VERSION: "20.x"

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      # 레포지토리 체크아웃
      - name: Checkout
        uses: actions/checkout@v4

      # Node.js 설치
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"

      # pnpm 설치
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      # pnpm store 캐시 경로 확인
      - name: Get pnpm store path
        id: pnpm_store
        shell: bash
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      # pnpm 캐시
      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm_store.outputs.path }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      # 의존성 설치
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 타입 체크 (전체)
      - name: Type check
        run: pnpm type-check
        # ex) tsc --noEmit 또는 각 앱별 type-check 스크립트

      # Lint
      - name: Lint
        run: pnpm lint

      # Unit Test (Vitest)
      - name: Test
        run: pnpm test
        # vitest.config.ts 기준

      # Build (필요한 앱만)
      - name: Build
        run: |
          pnpm --filter ./apps/web build
          pnpm --filter ./apps/admin build
        # api/worker는 런타임 빌드가 필요하면 추가

      # 빌드 결과 업로드 (선택)
      - name: Upload build artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: |
            apps/**/out
            apps/**/.next
          retention-days: 1
